name: pln-env
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - tensorflow
      - tensorboard
      - mlflow==2.19.0
